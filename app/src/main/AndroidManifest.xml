<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xk.customview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.CircleAndHeartActivity" />
        <activity android:name=".activity.LeafProgressActivity" />
        <activity android:name=".activity.MagicCircleActivity" />
        <activity android:name=".activity.SpiderActivity" />
        <activity android:name=".activity.InstrumentActivity" />
        <activity android:name=".activity.RoationActivity" />
        <activity android:name=".activity.SearchActivity" />
        <activity android:name=".activity.ZoomImageViewPagerActivity" />
        <activity android:name=".activity.FoldingLayoutActivity" />
        <activity android:name=".activity.DemoActivity" />
        <activity android:name=".activity.QQBubbleActivity">
        </activity>
    </application>

</manifest>